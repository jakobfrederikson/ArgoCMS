@page
@model ArgoCMS.Pages.Admin.Comments.NoticeComments.IndexModel

@{
    ViewData["Title"] = "Notice Comments";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Admin/Notice Comments</h1>
<partial name="_AdminNavPartial" />

<h1>Notice Comments</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-light">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoticeComment[0].Owner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoticeComment[0].CommentText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoticeComment[0].CreationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoticeComment[0].Notice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.NoticeComment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Owner.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommentText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notice.NoticeTitle)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CommentId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CommentId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CommentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
